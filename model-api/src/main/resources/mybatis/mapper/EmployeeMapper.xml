<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="me.luocaca.model.mapper.EmployeeMapper">


    <!--<select id="getEmpById" resultType="me.luocaca.model.entity.Employee">-->
        <!--SELECT * FROM  employee WHERE id=#{id}-->
    <!--</select>-->


    <insert id="insertEmp">
        INSERT INTO employee(last_name, email, gender,department_id )
         VALUES
         (#{lastName},#{email},#{gender},#{departmentId})
    </insert>


</mapper>